public class MainApp {
    public static void main(String[] args) {
        // Show the login form as the starting point
        LoginForm loginForm = new LoginForm();
        loginForm.setVisible(true);
    }
}
